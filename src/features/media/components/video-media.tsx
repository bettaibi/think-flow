/** This component would list all videos papers
 *
 * Note: display video, audio should be used inside a media story dialog component
 */

import { MediaProps } from "../types";

export function VideoMedia({ videos }: { videos: MediaProps[] }) {
  return <div>media-folders</div>;
}
