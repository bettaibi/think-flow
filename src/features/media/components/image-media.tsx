/** This component would list all videos papers
 *
 * Note: once clicked, display images in a full screen modal dialog, with zoom, next, previous, etc.
 */

import { MediaProps } from "../types";

export function ImageMedia({ images }: { images: MediaProps[] }) {
  return <div>media-folders</div>;
}
